{"title":"Physical Threshold Selection - Don River","markdown":{"yaml":{"title":"Physical Threshold Selection - Don River","author":"Dylan","date":"`r Sys.Date()`","output":"html_document"},"headingText":"Overview","containsRefs":false,"markdown":"\n```{r setup, include=FALSE}\nknitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE)\n```\n\n\n\nThis notebook documents how the physical flood threshold u_physical\nwas selected for the Don River at Todmorden (02HC024). u_physical is\nthe flow level in m³/s at which water leaves the Don River channel\nand causes real world damage. It is used to define flood events for\nthe frequency model and as the lower bound for the Bayesian MRL\ngrid search in the next notebook.\n\nThe process was not straightforward. The initial approach attempted\nto use the official TRCA flood warning threshold but was abandoned\ndue to an irreconcilable reference system mismatch between TRCA and\nEnvironment Canada data. Three independent empirical approaches were\nthen used to converge on a defensible value. Each approach and its\nlimitations are documented transparently below.\n\n---\n\n## 1. Load Data\n```{r}\nlibrary(tidyverse)\nlibrary(here)\n\nflow_data    <- read_csv(here::here(\"data\", \"clean\", \"02HC024_clean.csv\"))\nrating_curve <- read_csv(here::here(\"data\", \"clean\", \"02HC024_rating_curve_clean.csv\"))\n```\n\n---\n\n## 2. Understanding The Water Level Data And The TRCA Problem\n\nThe Environment Canada water level data uses a local gauge datum,\nnot elevation above sea level. For 02HC024 Todmorden the normal\nwater level reads approximately **12.1m** in the EC data. TRCA's\nreal-time gauging system reports the same station using metres\nabove sea level (MASL):\n```{r}\ntrca_levels <- tibble(\n  level       = c(\"Normal\", \"Low Alarm\", \"High Alarm\", \"High-High Alarm\"),\n  water_level = c(77.59, 78.70, 79.00, 79.30),\n  reference   = \"TRCA Real-Time Gauging (MASL)\"\n)\ntrca_levels\n```\n\nThe High Alarm level of **79.0m** MASL was the natural starting point\nas TRCA defines this as the level at which active flooding occurs.\nThe implied datum offset of **65.4m** was estimated by taking the\ndifference between the two normal level readings: TRCA reports\n77.59m MASL while Environment Canada records 12.21m for the same\nnormal conditions, giving 77.59 minus 12.21 equals 65.38m.\n**Converting the High Alarm level to local datum then gives approximately 13.6m (79.0 minus 65.4), suggesting 14.0m** as a\nreasonable working estimate for the flood threshold in EC units.\nHowever this offset was estimated not verified against an official\nbenchmark elevation document, making 14.0m an approximation rather\nthan an authoritative value.\n\nThe water level data covers 2002 onward and has a maximum observed\nvalue of `r round(max(rating_curve$water_level_m), 2)`m, which never\nreached the estimated 14.0m flood threshold. This means the\nthreshold cannot be validated against observed discharge data.\n\nAll three approaches below therefore work directly from the\ndischarge record. A future improvement would be to contact TRCA\nor Environment Canada directly to obtain the official benchmark\nelevation for 02HC024, which would allow direct conversion and\nindependent validation of u_physical against the official warning\nthreshold.\n\n---\n\n## 3. Rating Curve — Water Level vs Discharge\n```{r}\nggplot(rating_curve, aes(x = water_level_m, y = discharge_m3s)) +\n  geom_point(alpha = 0.3, size = 0.8, colour = \"steelblue\") +\n  geom_smooth(method = \"loess\", colour = \"red\",\n              se = TRUE, alpha = 0.1) +\n  geom_vline(xintercept = 14.0, colour = \"black\",\n             linetype = \"dashed\") +\n  annotate(\"text\", x = 14.05, y = 60,\n           label = \"Estimated flood\\nlevel (14.0m)\",\n           hjust = 0, size = 3) +\n  annotate(\"rect\", xmin = 13.55, xmax = 14.3,\n           ymin = 0, ymax = Inf,\n           alpha = 0.05, fill = \"red\") +\n  theme_minimal() +\n  labs(\n    title    = \"Rating Curve: Don River at Todmorden\",\n    subtitle = \"Water level vs discharge (2002 onward). Shaded region = beyond observed data range.\",\n    x        = \"Water Level (m, local datum)\",\n    y        = \"Discharge (m³/s)\"\n  )\n```\n\nThe rating curve shows a clear positive relationship between water\nlevel and discharge. The estimated flood threshold of 14.0m lies\nin the shaded region beyond the observed data range, making any\nextrapolation unreliable.\n\n---\n\n## 4. Highest Observed Water Level Events\n```{r}\nrating_curve %>%\n  arrange(desc(water_level_m)) %>%\n  select(YEAR, month_name, DD, water_level_m, discharge_m3s) %>%\n  head(20)\n```\n\nThe highest observed water levels between 13.0 and 13.5m correspond\nto discharge values of approximately 40 to 80 m³/s. The 2013 July\nflood appears at 53.3 m³/s, consistent with what HYDAT records for\nthat date.\n\n---\n\n## 5. Approach 1: Extrapolation From Rating Curve\n```{r}\nrating_model <- lm(log(discharge_m3s) ~ water_level_m,\n                   data = rating_curve)\n\npredicted      <- predict(rating_model,\n                          newdata = data.frame(water_level_m = 14.0))\n\n(u_extrapolated <- round(exp(predicted), 1))\n```\n\nThe log-linear extrapolation to 14.0m produces an estimated\ndischarge of `r u_extrapolated` m³/s, a physically impossible\nresult. The Amazon River flows at around 200,000 m³/s and\nthe Don River at its worst recorded event was approximately 400\nto 500 m³/s. This confirms that extrapolating 0.5m beyond the\nobserved water level range is unreliable. Hence, approach 1 is a discarded.\n\n---\n\n## 6. Approach 2: Known Historical Flood Events\n\nToronto's Don River has a well documented flood history. The\nfollowing events were identified through Environment Canada\ndischarge records, IBC catastrophe loss reports, and the\nCanadian Disaster Database:\n```{r}\nflood_events <- tibble(\n  date   = as.Date(c(\n    \"1954-10-15\",\n    \"1986-04-14\",\n    \"2013-07-08\",\n    \"2017-05-04\",\n    \"2019-05-24\"\n  )),\n  event  = c(\n    \"Hurricane Hazel\",\n    \"Spring freshet flooding\",\n    \"July thunderstorm — Toronto flash flood\",\n    \"Spring freshet flooding\",\n    \"Spring freshet flooding\"\n  ),\n  type   = c(\n    \"Fluvial\",\n    \"Fluvial\",\n    \"Pluvial/Fluvial\",\n    \"Fluvial\",\n    \"Fluvial\"\n  ),\n  source = c(\n    \"Canadian Disaster Database\",\n    \"Environment Canada discharge record\",\n    \"IBC — $940M insured loss (Ontario)\",\n    \"Environment Canada discharge record\",\n    \"Environment Canada discharge record\"\n  )\n)\n\nflood_events\n```\n\nCross referencing these dates against the HYDAT flow data:\n```{r}\nconfirmed_flood_flows <- flow_data %>%\n  filter(date %in% flood_events$date) %>%\n  filter(!is.na(flow)) %>%\n  left_join(flood_events, by = \"date\") %>%\n  select(date, event, type, flow)\n\nconfirmed_flood_flows\n```\n\nThe 2013 event records a daily mean of\n`r round(max(confirmed_flood_flows$flow), 1)` m³/s, the highest\nvalue for any known flood date in the HYDAT record. The 2017 and\n2019 values appear low. This is a consequence of using daily mean\ndischarge.(flood that peaked for 6 to 12 hours gets averaged\nacross the full 24 hour day, significantly understating the true\npeak flow)\n\n\nHurricane Hazel (1954) is absent from the HYDAT comparison because\nrecords for 02HC024 only begin in 1961. Hazel is historically\ndocumented as the most catastrophic flood event on the Don River\nwith peak flows on the Humber River reaching approximately 1,700\nm³/s. Its inclusion here acknowledges the existence of extreme tail\nevents beyond the observed record, directly relevant to the GPD\nshape parameter discussion in the severity model notebook.\n\nThe minimum daily mean across all confirmed flood dates where HYDAT\ndata exists is `r round(min(confirmed_flood_flows$flow), 1)` m³/s\nand the maximum is `r round(max(confirmed_flood_flows$flow), 1)`\nm³/s. These are daily means, true instantaneous peaks were\nsubstantially higher. u_physical should be below this minimum to\nensure all genuine flood events are captured while remaining above\nnormal flow conditions.\n\nThe 2013 event is classified as primarily **pluvial**: the discharge\nrecorded at the gauge reflects a combination of surface runoff and\nriverine flow that cannot be separated at daily resolution. This\nevent is included for completeness and as an upper bound reference\nbut is not used as a primary anchor for u_physical given the\nfluvial focus of this model. The threshold is derived primarily from the percentile analysis and sensitivity table in Sections 7 and 8.\n\n---\n\n## 7. Approach 3: Flow Percentiles\n```{r}\np_flow_tbl <- flow_data %>%\n  summarise(\n    p90  = quantile(flow, 0.90,  na.rm = TRUE),\n    p95  = quantile(flow, 0.95,  na.rm = TRUE),\n    p99  = quantile(flow, 0.99,  na.rm = TRUE),\n    p999 = quantile(flow, 0.999, na.rm = TRUE),\n    max  = max(flow, na.rm = TRUE)\n  ) %>%\n  pivot_longer(\n    cols      = everything(),\n    names_to  = \"percentile\",\n    values_to = \"flow_m3s\"\n  ) %>%\n  mutate(percentile = factor(percentile,\n                             levels = c(\"p90\", \"p95\", \"p99\",\n                                        \"p999\", \"max\")))\n\nggplot(p_flow_tbl, aes(x = percentile, y = flow_m3s, group = 1)) +\n  geom_line(colour = \"blue\", linewidth = 1) +\n  geom_point(colour = \"blue\", size = 3) +\n  geom_hline(yintercept = 30, colour = \"red\",\n             linetype = \"dashed\") +\n  annotate(\"text\", x = 1.2, y = 32,\n         label = \"Initial estimate: 30 m³/s\",\n         colour = \"red\", size = 3, hjust = 0) +\n  theme_minimal() +\n  labs(\n    title    = \"Flow Percentiles — Don River at Todmorden\",\n    subtitle = \"Exponential growth at upper tail confirms heavy tailed behavior\",\n    x        = \"Percentile\",\n    y        = \"Daily Mean Flow (m³/s)\"\n  )\n```\n```{r}\np_flow_tbl\n```\n\nThe jump from p99 to p99.9 is nearly double the jump from p95 to\np99, and the jump from p99.9 to the maximum is larger still. This\naccelerating growth at the upper tail is the visual signature of\nheavy tailed behavior and directly supports GPD as the appropriate\nseverity model.\n\n---\n\n## 8. Threshold Decision\n\n### Sensitivity Analysis\n\nEvent counts were checked across a range of candidate thresholds\nbefore committing to 30 m³/s:\n```{r}\nsensitivity <- tibble(threshold = c(20, 25, 30, 35, 40)) %>%\n  mutate(\n    days_above   = map_int(threshold, ~sum(flow_data$flow >= .x,\n                                           na.rm = TRUE)),\n    pct_above    = round(days_above / nrow(flow_data) * 100, 2),\n    rough_events = map_int(threshold, ~{\n      flow_data %>%\n        mutate(\n          above     = flow >= .x,\n          new_event = above & !lag(above, default = FALSE)\n        ) %>%\n        pull(new_event) %>%\n        sum(na.rm = TRUE)\n    })\n  )\n\nsensitivity\n```\n\nA threshold of 30 m³/s sits in the sweet spot - **original thought**... \n\nhigh enough to exclude normal elevated flow, low enough to retain sufficient\nevents for GPD fitting, and consistent with the onset of\nflood-producing conditions observed in the rating curve and\nhistorical event record.\n\n### First Decision\n```{r}\nU_PHYSICAL <- 30\n```\n\nu_physical = `r U_PHYSICAL` m³/s is selected for the following\nreasons. It sits above the p99 of `r round(quantile(flow_data$flow, 0.99, na.rm = TRUE), 1)` m³/s\nmeaning flows above 30 m³/s are rare, occurring on only\n`r round(mean(flow_data$flow >= U_PHYSICAL, na.rm = TRUE) * 100, 2)`%\nof days. It is consistent with observed flood events where all\ndocumented events in the rating curve correspond to daily means of\n40 to 80 m³/s, making 30 m³/s a conservative lower bound for\nflood-producing flows. It provides sufficient events for modeling\nwith approximately 40 to 60 independent events expected after\nproper declustering.\n\nThe known limitation is that u_physical is defined in terms of\ndaily mean discharge. True instantaneous flood peaks are higher.\nThis conservative threshold partially compensates by capturing\nthe onset of flood conditions rather than peak conditions.\n\nNote that the event count here is a rough upper bound based on\nthreshold crossings, not proper declustering. The full\ndeclustering algorithm using a 3 day minimum separation is\nimplemented in notebook 03_event_extraction.Rmd.\n\n\n### IMPORTANT CHANGE On u_physical After Declustering Analysis\n\nAfter implementing the declustering function the following event\ncharacteristics were observed at u_physical = 30 m³/s:\n\n```{r}\n\ninvisible(source(here::here(\"R_functions\", \"declustering.R\"), echo = FALSE))\n\nflood_events_30 <- decluster_events(\n  flow_data            = flow_data,\n  threshold            = 30,\n  min_days_independent = 3\n)\n\nflood_events_30 %>%\n  summarise(\n    n_events        = n(),\n    mean_duration   = mean(duration),\n    median_duration = median(duration),\n    max_duration    = max(duration)\n  )\n\nflood_events_30 %>%\n  arrange(start_date) %>%\n  mutate(days_since_last = as.numeric(start_date - lag(end_date))) %>%\n  summarise(\n    mean_gap    = mean(days_since_last, na.rm = TRUE),\n    median_gap  = median(days_since_last, na.rm = TRUE),\n    min_gap     = min(days_since_last, na.rm = TRUE),\n    pct_under_3 = mean(days_since_last < 3, na.rm = TRUE) * 100\n  )\n```\n\nThe median event duration of 1 day confirms that the Don River\nresponds and recovers rapidly at 30 m³/s, consistent with a\nsmall urban catchment with fast drainage. The minimum inter-event\ngap of 4 days and zero events separated by fewer than 3 days\nconfirms all 129 events are independent. The 3 day\nseparation criterion was therefore conservative for this dataset as natural event separation was already occurring.\n\nHowever the 1 day median duration brings a physical concern.\nA single day spike barely above 30 m³/s is unlikely to represent\na damaging flood event. The rating curve analysis in\nSection 4 showed all documented flood events correspond to daily\nmeans of 40 to 80 m³/s. Events between 30 and 40 m³/s likely\nrepresent elevated flow conditions rather than active flooding.\n\nComparing event characteristics across candidate thresholds:\n```{r}\nthreshold_comparison <- tibble(threshold = c(30, 35, 40, 45, 50)) %>%\n  mutate(\n    n_events = map_int(threshold, \\(u) {\n      decluster_events(flow_data, u, 3) %>% nrow()\n    }),\n    median_duration = map_int(threshold, \\(u) {\n      events <- decluster_events(flow_data, u, 3)\n      as.integer(median(events$duration))\n    }),\n    mean_Q_peak = map_dbl(threshold, \\(u) {\n      events <- decluster_events(flow_data, u, 3)\n      round(mean(events$Q_peak), 1)\n    })\n  )\n\nthreshold_comparison\n```\n\nRaising u_physical to 40 m³/s produces 54 independent events\nwith a higher mean Q_peak of 53.4 m³/s, better aligned with\nthe documented flood damage range of 40 to 80 m³/s observed\nin the rating curve. The median event duration remains 1 day\nacross all candidate thresholds, a consistent characteristic\nof the Don River as a fast responding urban catchment rather\nthan a product of the threshold choice.\n\n54 events sits at the boundary of the practical minimum of 50\nexceedances recommended by Coles (2001) for reliable GPD parameter estimation. The Bayesian framework partially relaxes this requirement through prior\nregularization, but 40 m³/s is the highest threshold that\nremains statistically workable. Raising to 45 m³/s drops event\ncount to 35, below the minimum regardless of framework.\n\nu_physical is revised from 30 to 40 m³/s on **physical grounds**.\nAt **30 m³/s** the mean Q_peak of 42.2 m³/s and **single day median duration suggest many events represent brief elevated flow rather than genuine flooding**. At **40 m³/s** the mean Q_peak of 53.4 m³/s is consistent with the onset of documented flood\ndamage and the threshold sits **above the p99.9 of 52.5 m³/s**,\ncapturing only genuinely extreme events.\n\n## 9. Save u_physical\n```{r}\nU_PHYSICAL <- 40\n\nflood_events <- decluster_events(\n  flow_data            = flow_data,\n  threshold            = U_PHYSICAL,\n  min_days_independent = 3\n)\n\nsaveRDS(U_PHYSICAL,\n        here::here(\"data\", \"clean\", \"u_physical.rds\"))\n\nwrite_csv(flood_events,\n          here::here(\"data\", \"clean\", \"flood_events.csv\"))\n```\n\nu_physical updated to `r U_PHYSICAL` m³/s and saved. The flood\nevents table has been updated accordingly with\n`r nrow(flood_events)` independent events.\n\n---\n\n## Summary\n\n| u_physical | 40 m³/s | Above p99.9, mean Q_peak consistent with documented flood damage, 54 independent events above GPD minimum |","srcMarkdownNoYaml":"\n```{r setup, include=FALSE}\nknitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE)\n```\n\n\n## Overview\n\nThis notebook documents how the physical flood threshold u_physical\nwas selected for the Don River at Todmorden (02HC024). u_physical is\nthe flow level in m³/s at which water leaves the Don River channel\nand causes real world damage. It is used to define flood events for\nthe frequency model and as the lower bound for the Bayesian MRL\ngrid search in the next notebook.\n\nThe process was not straightforward. The initial approach attempted\nto use the official TRCA flood warning threshold but was abandoned\ndue to an irreconcilable reference system mismatch between TRCA and\nEnvironment Canada data. Three independent empirical approaches were\nthen used to converge on a defensible value. Each approach and its\nlimitations are documented transparently below.\n\n---\n\n## 1. Load Data\n```{r}\nlibrary(tidyverse)\nlibrary(here)\n\nflow_data    <- read_csv(here::here(\"data\", \"clean\", \"02HC024_clean.csv\"))\nrating_curve <- read_csv(here::here(\"data\", \"clean\", \"02HC024_rating_curve_clean.csv\"))\n```\n\n---\n\n## 2. Understanding The Water Level Data And The TRCA Problem\n\nThe Environment Canada water level data uses a local gauge datum,\nnot elevation above sea level. For 02HC024 Todmorden the normal\nwater level reads approximately **12.1m** in the EC data. TRCA's\nreal-time gauging system reports the same station using metres\nabove sea level (MASL):\n```{r}\ntrca_levels <- tibble(\n  level       = c(\"Normal\", \"Low Alarm\", \"High Alarm\", \"High-High Alarm\"),\n  water_level = c(77.59, 78.70, 79.00, 79.30),\n  reference   = \"TRCA Real-Time Gauging (MASL)\"\n)\ntrca_levels\n```\n\nThe High Alarm level of **79.0m** MASL was the natural starting point\nas TRCA defines this as the level at which active flooding occurs.\nThe implied datum offset of **65.4m** was estimated by taking the\ndifference between the two normal level readings: TRCA reports\n77.59m MASL while Environment Canada records 12.21m for the same\nnormal conditions, giving 77.59 minus 12.21 equals 65.38m.\n**Converting the High Alarm level to local datum then gives approximately 13.6m (79.0 minus 65.4), suggesting 14.0m** as a\nreasonable working estimate for the flood threshold in EC units.\nHowever this offset was estimated not verified against an official\nbenchmark elevation document, making 14.0m an approximation rather\nthan an authoritative value.\n\nThe water level data covers 2002 onward and has a maximum observed\nvalue of `r round(max(rating_curve$water_level_m), 2)`m, which never\nreached the estimated 14.0m flood threshold. This means the\nthreshold cannot be validated against observed discharge data.\n\nAll three approaches below therefore work directly from the\ndischarge record. A future improvement would be to contact TRCA\nor Environment Canada directly to obtain the official benchmark\nelevation for 02HC024, which would allow direct conversion and\nindependent validation of u_physical against the official warning\nthreshold.\n\n---\n\n## 3. Rating Curve — Water Level vs Discharge\n```{r}\nggplot(rating_curve, aes(x = water_level_m, y = discharge_m3s)) +\n  geom_point(alpha = 0.3, size = 0.8, colour = \"steelblue\") +\n  geom_smooth(method = \"loess\", colour = \"red\",\n              se = TRUE, alpha = 0.1) +\n  geom_vline(xintercept = 14.0, colour = \"black\",\n             linetype = \"dashed\") +\n  annotate(\"text\", x = 14.05, y = 60,\n           label = \"Estimated flood\\nlevel (14.0m)\",\n           hjust = 0, size = 3) +\n  annotate(\"rect\", xmin = 13.55, xmax = 14.3,\n           ymin = 0, ymax = Inf,\n           alpha = 0.05, fill = \"red\") +\n  theme_minimal() +\n  labs(\n    title    = \"Rating Curve: Don River at Todmorden\",\n    subtitle = \"Water level vs discharge (2002 onward). Shaded region = beyond observed data range.\",\n    x        = \"Water Level (m, local datum)\",\n    y        = \"Discharge (m³/s)\"\n  )\n```\n\nThe rating curve shows a clear positive relationship between water\nlevel and discharge. The estimated flood threshold of 14.0m lies\nin the shaded region beyond the observed data range, making any\nextrapolation unreliable.\n\n---\n\n## 4. Highest Observed Water Level Events\n```{r}\nrating_curve %>%\n  arrange(desc(water_level_m)) %>%\n  select(YEAR, month_name, DD, water_level_m, discharge_m3s) %>%\n  head(20)\n```\n\nThe highest observed water levels between 13.0 and 13.5m correspond\nto discharge values of approximately 40 to 80 m³/s. The 2013 July\nflood appears at 53.3 m³/s, consistent with what HYDAT records for\nthat date.\n\n---\n\n## 5. Approach 1: Extrapolation From Rating Curve\n```{r}\nrating_model <- lm(log(discharge_m3s) ~ water_level_m,\n                   data = rating_curve)\n\npredicted      <- predict(rating_model,\n                          newdata = data.frame(water_level_m = 14.0))\n\n(u_extrapolated <- round(exp(predicted), 1))\n```\n\nThe log-linear extrapolation to 14.0m produces an estimated\ndischarge of `r u_extrapolated` m³/s, a physically impossible\nresult. The Amazon River flows at around 200,000 m³/s and\nthe Don River at its worst recorded event was approximately 400\nto 500 m³/s. This confirms that extrapolating 0.5m beyond the\nobserved water level range is unreliable. Hence, approach 1 is a discarded.\n\n---\n\n## 6. Approach 2: Known Historical Flood Events\n\nToronto's Don River has a well documented flood history. The\nfollowing events were identified through Environment Canada\ndischarge records, IBC catastrophe loss reports, and the\nCanadian Disaster Database:\n```{r}\nflood_events <- tibble(\n  date   = as.Date(c(\n    \"1954-10-15\",\n    \"1986-04-14\",\n    \"2013-07-08\",\n    \"2017-05-04\",\n    \"2019-05-24\"\n  )),\n  event  = c(\n    \"Hurricane Hazel\",\n    \"Spring freshet flooding\",\n    \"July thunderstorm — Toronto flash flood\",\n    \"Spring freshet flooding\",\n    \"Spring freshet flooding\"\n  ),\n  type   = c(\n    \"Fluvial\",\n    \"Fluvial\",\n    \"Pluvial/Fluvial\",\n    \"Fluvial\",\n    \"Fluvial\"\n  ),\n  source = c(\n    \"Canadian Disaster Database\",\n    \"Environment Canada discharge record\",\n    \"IBC — $940M insured loss (Ontario)\",\n    \"Environment Canada discharge record\",\n    \"Environment Canada discharge record\"\n  )\n)\n\nflood_events\n```\n\nCross referencing these dates against the HYDAT flow data:\n```{r}\nconfirmed_flood_flows <- flow_data %>%\n  filter(date %in% flood_events$date) %>%\n  filter(!is.na(flow)) %>%\n  left_join(flood_events, by = \"date\") %>%\n  select(date, event, type, flow)\n\nconfirmed_flood_flows\n```\n\nThe 2013 event records a daily mean of\n`r round(max(confirmed_flood_flows$flow), 1)` m³/s, the highest\nvalue for any known flood date in the HYDAT record. The 2017 and\n2019 values appear low. This is a consequence of using daily mean\ndischarge.(flood that peaked for 6 to 12 hours gets averaged\nacross the full 24 hour day, significantly understating the true\npeak flow)\n\n\nHurricane Hazel (1954) is absent from the HYDAT comparison because\nrecords for 02HC024 only begin in 1961. Hazel is historically\ndocumented as the most catastrophic flood event on the Don River\nwith peak flows on the Humber River reaching approximately 1,700\nm³/s. Its inclusion here acknowledges the existence of extreme tail\nevents beyond the observed record, directly relevant to the GPD\nshape parameter discussion in the severity model notebook.\n\nThe minimum daily mean across all confirmed flood dates where HYDAT\ndata exists is `r round(min(confirmed_flood_flows$flow), 1)` m³/s\nand the maximum is `r round(max(confirmed_flood_flows$flow), 1)`\nm³/s. These are daily means, true instantaneous peaks were\nsubstantially higher. u_physical should be below this minimum to\nensure all genuine flood events are captured while remaining above\nnormal flow conditions.\n\nThe 2013 event is classified as primarily **pluvial**: the discharge\nrecorded at the gauge reflects a combination of surface runoff and\nriverine flow that cannot be separated at daily resolution. This\nevent is included for completeness and as an upper bound reference\nbut is not used as a primary anchor for u_physical given the\nfluvial focus of this model. The threshold is derived primarily from the percentile analysis and sensitivity table in Sections 7 and 8.\n\n---\n\n## 7. Approach 3: Flow Percentiles\n```{r}\np_flow_tbl <- flow_data %>%\n  summarise(\n    p90  = quantile(flow, 0.90,  na.rm = TRUE),\n    p95  = quantile(flow, 0.95,  na.rm = TRUE),\n    p99  = quantile(flow, 0.99,  na.rm = TRUE),\n    p999 = quantile(flow, 0.999, na.rm = TRUE),\n    max  = max(flow, na.rm = TRUE)\n  ) %>%\n  pivot_longer(\n    cols      = everything(),\n    names_to  = \"percentile\",\n    values_to = \"flow_m3s\"\n  ) %>%\n  mutate(percentile = factor(percentile,\n                             levels = c(\"p90\", \"p95\", \"p99\",\n                                        \"p999\", \"max\")))\n\nggplot(p_flow_tbl, aes(x = percentile, y = flow_m3s, group = 1)) +\n  geom_line(colour = \"blue\", linewidth = 1) +\n  geom_point(colour = \"blue\", size = 3) +\n  geom_hline(yintercept = 30, colour = \"red\",\n             linetype = \"dashed\") +\n  annotate(\"text\", x = 1.2, y = 32,\n         label = \"Initial estimate: 30 m³/s\",\n         colour = \"red\", size = 3, hjust = 0) +\n  theme_minimal() +\n  labs(\n    title    = \"Flow Percentiles — Don River at Todmorden\",\n    subtitle = \"Exponential growth at upper tail confirms heavy tailed behavior\",\n    x        = \"Percentile\",\n    y        = \"Daily Mean Flow (m³/s)\"\n  )\n```\n```{r}\np_flow_tbl\n```\n\nThe jump from p99 to p99.9 is nearly double the jump from p95 to\np99, and the jump from p99.9 to the maximum is larger still. This\naccelerating growth at the upper tail is the visual signature of\nheavy tailed behavior and directly supports GPD as the appropriate\nseverity model.\n\n---\n\n## 8. Threshold Decision\n\n### Sensitivity Analysis\n\nEvent counts were checked across a range of candidate thresholds\nbefore committing to 30 m³/s:\n```{r}\nsensitivity <- tibble(threshold = c(20, 25, 30, 35, 40)) %>%\n  mutate(\n    days_above   = map_int(threshold, ~sum(flow_data$flow >= .x,\n                                           na.rm = TRUE)),\n    pct_above    = round(days_above / nrow(flow_data) * 100, 2),\n    rough_events = map_int(threshold, ~{\n      flow_data %>%\n        mutate(\n          above     = flow >= .x,\n          new_event = above & !lag(above, default = FALSE)\n        ) %>%\n        pull(new_event) %>%\n        sum(na.rm = TRUE)\n    })\n  )\n\nsensitivity\n```\n\nA threshold of 30 m³/s sits in the sweet spot - **original thought**... \n\nhigh enough to exclude normal elevated flow, low enough to retain sufficient\nevents for GPD fitting, and consistent with the onset of\nflood-producing conditions observed in the rating curve and\nhistorical event record.\n\n### First Decision\n```{r}\nU_PHYSICAL <- 30\n```\n\nu_physical = `r U_PHYSICAL` m³/s is selected for the following\nreasons. It sits above the p99 of `r round(quantile(flow_data$flow, 0.99, na.rm = TRUE), 1)` m³/s\nmeaning flows above 30 m³/s are rare, occurring on only\n`r round(mean(flow_data$flow >= U_PHYSICAL, na.rm = TRUE) * 100, 2)`%\nof days. It is consistent with observed flood events where all\ndocumented events in the rating curve correspond to daily means of\n40 to 80 m³/s, making 30 m³/s a conservative lower bound for\nflood-producing flows. It provides sufficient events for modeling\nwith approximately 40 to 60 independent events expected after\nproper declustering.\n\nThe known limitation is that u_physical is defined in terms of\ndaily mean discharge. True instantaneous flood peaks are higher.\nThis conservative threshold partially compensates by capturing\nthe onset of flood conditions rather than peak conditions.\n\nNote that the event count here is a rough upper bound based on\nthreshold crossings, not proper declustering. The full\ndeclustering algorithm using a 3 day minimum separation is\nimplemented in notebook 03_event_extraction.Rmd.\n\n\n### IMPORTANT CHANGE On u_physical After Declustering Analysis\n\nAfter implementing the declustering function the following event\ncharacteristics were observed at u_physical = 30 m³/s:\n\n```{r}\n\ninvisible(source(here::here(\"R_functions\", \"declustering.R\"), echo = FALSE))\n\nflood_events_30 <- decluster_events(\n  flow_data            = flow_data,\n  threshold            = 30,\n  min_days_independent = 3\n)\n\nflood_events_30 %>%\n  summarise(\n    n_events        = n(),\n    mean_duration   = mean(duration),\n    median_duration = median(duration),\n    max_duration    = max(duration)\n  )\n\nflood_events_30 %>%\n  arrange(start_date) %>%\n  mutate(days_since_last = as.numeric(start_date - lag(end_date))) %>%\n  summarise(\n    mean_gap    = mean(days_since_last, na.rm = TRUE),\n    median_gap  = median(days_since_last, na.rm = TRUE),\n    min_gap     = min(days_since_last, na.rm = TRUE),\n    pct_under_3 = mean(days_since_last < 3, na.rm = TRUE) * 100\n  )\n```\n\nThe median event duration of 1 day confirms that the Don River\nresponds and recovers rapidly at 30 m³/s, consistent with a\nsmall urban catchment with fast drainage. The minimum inter-event\ngap of 4 days and zero events separated by fewer than 3 days\nconfirms all 129 events are independent. The 3 day\nseparation criterion was therefore conservative for this dataset as natural event separation was already occurring.\n\nHowever the 1 day median duration brings a physical concern.\nA single day spike barely above 30 m³/s is unlikely to represent\na damaging flood event. The rating curve analysis in\nSection 4 showed all documented flood events correspond to daily\nmeans of 40 to 80 m³/s. Events between 30 and 40 m³/s likely\nrepresent elevated flow conditions rather than active flooding.\n\nComparing event characteristics across candidate thresholds:\n```{r}\nthreshold_comparison <- tibble(threshold = c(30, 35, 40, 45, 50)) %>%\n  mutate(\n    n_events = map_int(threshold, \\(u) {\n      decluster_events(flow_data, u, 3) %>% nrow()\n    }),\n    median_duration = map_int(threshold, \\(u) {\n      events <- decluster_events(flow_data, u, 3)\n      as.integer(median(events$duration))\n    }),\n    mean_Q_peak = map_dbl(threshold, \\(u) {\n      events <- decluster_events(flow_data, u, 3)\n      round(mean(events$Q_peak), 1)\n    })\n  )\n\nthreshold_comparison\n```\n\nRaising u_physical to 40 m³/s produces 54 independent events\nwith a higher mean Q_peak of 53.4 m³/s, better aligned with\nthe documented flood damage range of 40 to 80 m³/s observed\nin the rating curve. The median event duration remains 1 day\nacross all candidate thresholds, a consistent characteristic\nof the Don River as a fast responding urban catchment rather\nthan a product of the threshold choice.\n\n54 events sits at the boundary of the practical minimum of 50\nexceedances recommended by Coles (2001) for reliable GPD parameter estimation. The Bayesian framework partially relaxes this requirement through prior\nregularization, but 40 m³/s is the highest threshold that\nremains statistically workable. Raising to 45 m³/s drops event\ncount to 35, below the minimum regardless of framework.\n\nu_physical is revised from 30 to 40 m³/s on **physical grounds**.\nAt **30 m³/s** the mean Q_peak of 42.2 m³/s and **single day median duration suggest many events represent brief elevated flow rather than genuine flooding**. At **40 m³/s** the mean Q_peak of 53.4 m³/s is consistent with the onset of documented flood\ndamage and the threshold sits **above the p99.9 of 52.5 m³/s**,\ncapturing only genuinely extreme events.\n\n## 9. Save u_physical\n```{r}\nU_PHYSICAL <- 40\n\nflood_events <- decluster_events(\n  flow_data            = flow_data,\n  threshold            = U_PHYSICAL,\n  min_days_independent = 3\n)\n\nsaveRDS(U_PHYSICAL,\n        here::here(\"data\", \"clean\", \"u_physical.rds\"))\n\nwrite_csv(flood_events,\n          here::here(\"data\", \"clean\", \"flood_events.csv\"))\n```\n\nu_physical updated to `r U_PHYSICAL` m³/s and saved. The flood\nevents table has been updated accordingly with\n`r nrow(flood_events)` independent events.\n\n---\n\n## Summary\n\n| u_physical | 40 m³/s | Above p99.9, mean Q_peak consistent with documented flood damage, 54 independent events above GPD minimum |"},"formats":{"html":{"identifier":{"display-name":"HTML","target-format":"html","base-format":"html"},"execute":{"fig-width":7,"fig-height":5,"fig-format":"retina","fig-dpi":96,"df-print":"default","error":false,"eval":true,"cache":null,"freeze":false,"echo":true,"output":"html_document","warning":true,"include":true,"keep-md":false,"keep-ipynb":false,"ipynb":null,"enabled":null,"daemon":null,"daemon-restart":false,"debug":false,"ipynb-filters":[],"ipynb-shell-interactivity":null,"plotly-connected":true,"engine":"knitr"},"render":{"keep-tex":false,"keep-typ":false,"keep-source":false,"keep-hidden":false,"prefer-html":false,"output-divs":true,"output-ext":"html","fig-align":"default","fig-pos":null,"fig-env":null,"code-fold":"none","code-overflow":"scroll","code-link":false,"code-line-numbers":false,"code-tools":false,"tbl-colwidths":"auto","merge-includes":true,"inline-includes":false,"preserve-yaml":false,"latex-auto-mk":true,"latex-auto-install":true,"latex-clean":true,"latex-min-runs":1,"latex-max-runs":10,"latex-makeindex":"makeindex","latex-makeindex-opts":[],"latex-tlmgr-opts":[],"latex-input-paths":[],"latex-output-dir":null,"link-external-icon":false,"link-external-newwindow":false,"self-contained-math":false,"format-resources":[],"notebook-links":true},"pandoc":{"standalone":true,"wrap":"none","default-image-extension":"png","to":"html","output-file":"02_physical_threshold_selection.html"},"language":{"toc-title-document":"Table of contents","toc-title-website":"On this page","related-formats-title":"Other Formats","related-notebooks-title":"Notebooks","source-notebooks-prefix":"Source","other-links-title":"Other Links","code-links-title":"Code Links","launch-dev-container-title":"Launch Dev Container","launch-binder-title":"Launch Binder","article-notebook-label":"Article Notebook","notebook-preview-download":"Download Notebook","notebook-preview-download-src":"Download Source","notebook-preview-back":"Back to Article","manuscript-meca-bundle":"MECA Bundle","section-title-abstract":"Abstract","section-title-appendices":"Appendices","section-title-footnotes":"Footnotes","section-title-references":"References","section-title-reuse":"Reuse","section-title-copyright":"Copyright","section-title-citation":"Citation","appendix-attribution-cite-as":"For attribution, please cite this work as:","appendix-attribution-bibtex":"BibTeX citation:","appendix-view-license":"View License","title-block-author-single":"Author","title-block-author-plural":"Authors","title-block-affiliation-single":"Affiliation","title-block-affiliation-plural":"Affiliations","title-block-published":"Published","title-block-modified":"Modified","title-block-keywords":"Keywords","callout-tip-title":"Tip","callout-note-title":"Note","callout-warning-title":"Warning","callout-important-title":"Important","callout-caution-title":"Caution","code-summary":"Code","code-tools-menu-caption":"Code","code-tools-show-all-code":"Show All Code","code-tools-hide-all-code":"Hide All Code","code-tools-view-source":"View Source","code-tools-source-code":"Source Code","tools-share":"Share","tools-download":"Download","code-line":"Line","code-lines":"Lines","copy-button-tooltip":"Copy to Clipboard","copy-button-tooltip-success":"Copied!","repo-action-links-edit":"Edit this page","repo-action-links-source":"View source","repo-action-links-issue":"Report an issue","back-to-top":"Back to top","search-no-results-text":"No results","search-matching-documents-text":"matching documents","search-copy-link-title":"Copy link to search","search-hide-matches-text":"Hide additional matches","search-more-match-text":"more match in this document","search-more-matches-text":"more matches in this document","search-clear-button-title":"Clear","search-text-placeholder":"","search-detached-cancel-button-title":"Cancel","search-submit-button-title":"Submit","search-label":"Search","toggle-section":"Toggle section","toggle-sidebar":"Toggle sidebar navigation","toggle-dark-mode":"Toggle dark mode","toggle-reader-mode":"Toggle reader mode","toggle-navigation":"Toggle navigation","crossref-fig-title":"Figure","crossref-tbl-title":"Table","crossref-lst-title":"Listing","crossref-thm-title":"Theorem","crossref-lem-title":"Lemma","crossref-cor-title":"Corollary","crossref-prp-title":"Proposition","crossref-cnj-title":"Conjecture","crossref-def-title":"Definition","crossref-exm-title":"Example","crossref-exr-title":"Exercise","crossref-ch-prefix":"Chapter","crossref-apx-prefix":"Appendix","crossref-sec-prefix":"Section","crossref-eq-prefix":"Equation","crossref-lof-title":"List of Figures","crossref-lot-title":"List of Tables","crossref-lol-title":"List of Listings","environment-proof-title":"Proof","environment-remark-title":"Remark","environment-solution-title":"Solution","listing-page-order-by":"Order By","listing-page-order-by-default":"Default","listing-page-order-by-date-asc":"Oldest","listing-page-order-by-date-desc":"Newest","listing-page-order-by-number-desc":"High to Low","listing-page-order-by-number-asc":"Low to High","listing-page-field-date":"Date","listing-page-field-title":"Title","listing-page-field-description":"Description","listing-page-field-author":"Author","listing-page-field-filename":"File Name","listing-page-field-filemodified":"Modified","listing-page-field-subtitle":"Subtitle","listing-page-field-readingtime":"Reading Time","listing-page-field-wordcount":"Word Count","listing-page-field-categories":"Categories","listing-page-minutes-compact":"{0} min","listing-page-category-all":"All","listing-page-no-matches":"No matching items","listing-page-words":"{0} words","listing-page-filter":"Filter","draft":"Draft"},"metadata":{"lang":"en","fig-responsive":true,"quarto-version":"1.8.27","title":"Physical Threshold Selection - Don River","author":"Dylan","date":"`r Sys.Date()`"},"extensions":{"book":{"multiFile":true}}}},"projectFormats":[]}